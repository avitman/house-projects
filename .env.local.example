# Copy this file to .env.local (for `vercel dev`) and fill in your value.
# Also add the same variable in the Vercel project settings for deploys.

# The Google Apps Script Web App /exec URL (see apps-script/Code.gs + README
# for how to deploy it). This is what the backend reads/writes through.
SHEETS_WEBAPP_URL=
